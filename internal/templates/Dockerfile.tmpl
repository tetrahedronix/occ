# Generated by occ {{.OccVersion}} from {{.ConfigFile}} (sha256:{{.ConfigHash}})
# DO NOT EDIT: regenerate with `occ generate`.

FROM {{.BaseImage}}
{{- if .Packages}}

RUN {{if eq .PM "apk"}}apk add --no-cache{{else}}apt-get update && apt-get install -y --no-install-recommends{{end}}{{range .Packages}} {{.}}{{end}}{{if eq .PM "apt"}} \
    && rm -rf /var/lib/apt/lists/*{{end}}
{{- end}}
